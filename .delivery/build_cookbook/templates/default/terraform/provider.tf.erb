terraform {
  backend "azurerm" {
    storage_account_name = "terraformdelilock"
    container_name       = "environments"
    resource_group_name  = "dvo_terraform_deli"
    key                  = "<%= @workflow_cookbook_name %>/terraform.tfstate"
  }
}

provider "azurerm" {}
